using System;
namespace IntroDel1
{

    class Program
    {
        /*To  MAIN ting i en klasse:
         * Felt = NETSEN!!! JS GLobal Var
         * Methods = Function JS
         * 
         * --Grunnskjellet til en class
         * --Var
         * Debugging
         * --Tilordning > --Valgsetninger & Datatyper Decklarationbs
         *      int heltall
         *      float/double desimal
         *      string = string
         *      true/false
         * --Loops-
         * --Tables
         * --Funksjoner / Methods
         * -- Input Output
         */
        static void Main(string[] args)
        {



            /*
             * DataType Deklarasjoner: ---
             * Kan deklarere fler variabler : fex -- bool b1,b2,b3,b4;
             */
            bool b; //Boolean -- True/False
            int i; // Int -- Heltall
            float f; // Float -- desimal
            double d;
            string s; // String -- Text

            //Tilordninger --
            b = true; // eller false
            i = 13;
            f = 13.37f;
            d = 15.4;
            s = "Hei på deg";
            //Går ann og deklarere og tilordne samtidig
            bool b2 = false;
            int i2 = 4;
            float f2 = 4.5f;
            double d2 = 1.2;
            string s2 = "Snakkas!";
            var b3 = true;
            /* Kan bruke Var ^
             */

             



            //    if(args.Length == 0)
            //    {
            //    Console.WriteLine(args.Length + "--Parametere");
            //    }
            //    else
            //    {
            //        Console.WriteLine("Antall Parametere: " + args.Length);
            //    }
            //    foreach (var test in args)
            //    {
            //        //var parno = i + 1;
            //        Console.WriteLine("Parameter : " + test);
            //    }

            //}
        }
    }
}